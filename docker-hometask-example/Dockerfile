# базовый образ для контейнера
FROM mysql:latest

# настройка переменных окружения
ENV MYSQL_ROOT_PASSWORD=root
ENV MYSQL_DATABASE=users_db

# копирование скриптов для инициализации БД в директорию инициализации
# для того, чтобы выполнились скрипты 1 и 2
# чтобы они скопировались не по алфавиту а в том порядке,
# в котором мы их расположили - соберем их в одну папку
COPY ./db-scripts/*init.sql /docker-entrypoint-initdb.d/